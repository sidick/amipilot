# host

The host-side Python client: object API for driving the AmiPilot server
over serial or TCP, plus a pytest plugin (fixtures that boot an emulator
config, wait for the server, and expose the API).

Lands in phase 0.3 -- see
[`docs/implementation-plan.md`](../docs/implementation-plan.md).
