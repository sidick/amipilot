# Pinned docs-site toolchain (mkdocs.yml). Matches sibling projects
# amiauth/sana2loop's own pinned versions -- see those projects' notes if
# either ever needs a bump.
# Material 9.x targets MkDocs 1.x; do not float across the MkDocs 2.0 rewrite.
mkdocs-material==9.7.7
mike==2.2.0
