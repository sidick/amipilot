# tests

Host-side pytest suites -- the CI entry point once the host client (phase
0.3) exists. Empty for now; `AmiInspect`'s phase 0.1 verification runs
on-target against `fixtures/`, not from here.

See [`docs/implementation-plan.md`](../docs/implementation-plan.md).
