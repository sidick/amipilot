# version.mk -- the one place the AmiPilot version lives.
#
# VERSION is the Amiga major (what a compatibility-minded caller, or the
# wire protocol's version handshake, would read); REVISION is the minor.
# Started at 0.1 per docs/implementation-plan.md's phase 0.1 release gate.
VERSION  := 1
REVISION := 1
